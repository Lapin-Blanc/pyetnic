[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pyetnic"
version = "0.1.0b1"
description = "Access to ETNIC web services through Python"
readme = "README.md"
license = "MIT"
authors = [{ name = "Fabien Toune", email = "fabien.toune@eica.be" }]
requires-python = ">=3.10"
dependencies = [
    "zeep",
    "python-dotenv",
    "requests",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Operating System :: OS Independent",
    "Topic :: Software Development :: Libraries :: Python Modules",
]

[project.optional-dependencies]
seps = ["xmlsec", "cryptography"]
excel = ["openpyxl"]

[project.urls]
Homepage = "https://github.com/Lapin-Blanc/pyetnic"
Repository = "https://github.com/Lapin-Blanc/pyetnic"
Issues = "https://github.com/Lapin-Blanc/pyetnic/issues"
Changelog = "https://github.com/Lapin-Blanc/pyetnic/blob/main/CHANGELOG.md"

[project.scripts]
pyetnic = "pyetnic.cli:main"

[tool.setuptools.packages.find]
include = ["pyetnic*"]
exclude = ["tests*"]

[tool.setuptools.package-data]
pyetnic = [
    "py.typed",
    "resources/*/*.wsdl",
    "resources/*/xsd/*.xsd",
]

[tool.pytest.ini_options]
# Warning normal en mode dev (ENV=dev) : SSL désactivé pour les serveurs TQ
filterwarnings = ["ignore::urllib3.exceptions.InsecureRequestWarning"]
